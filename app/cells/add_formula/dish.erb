<div class="add-formula dish">
    <header>
        <h5 class="title -compact -faint">Plat:</h5>
        <h5 class="choice">Boeuf fusio bien cuit</h5>
    </header>
    <div class="col-12 -sp">
        <div class="item">
            <div class="col-6 -sp">
                <h6 class="subtitle">Garnitures</h6>
                <p></p>
            </div>
            <div class="col-6 -sp">
                <h6 class="subtitle">Marinades</h6>
                <p></p>
            </div>
        </div>
        <div class="item base">
            <h6 class="title -compact -faint">Base</h6>
            <% @dishes.each do |d| %>
                <%= @f.radio_button :dish, d.id, :checked => (d == @dishes.first) %>
                <label for="formula_dish_<%= d.id %>">
                    <div class="img">
                        <%= image_tag "https://i.ytimg.com/vi/uZSrcVS6EuM/hqdefault.jpg" %>
                    </div>
                    <h6><%= d.name %></h6>
                </label>
            <% end %>
        </div>
        <div class="item baking">
            <h6 class="title -compact -faint">Mode de Cuisson</h6>
            <% @bakings.each do |b| %>
                <%= @f.radio_button :baking, b.id, :checked => (b == @bakings.first) %>
                <label for="formula_baking_<%= b.id %>">
                    <div class="img">
                        <%= image_tag "https://i.ytimg.com/vi/uZSrcVS6EuM/hqdefault.jpg" %>
                    </div>
                    <h6><%= b.name %></h6>
                </label>
            <% end %>
            <div class="ingredients">

            </div>
        </div>
        <div class="item roasting">
            <h6 class="title -compact -faint">Cuisson</h6>
            <% @roastings.each do |r| %>
                <%= @f.radio_button :roasting, r, :checked => (r == @roastings.first) %>
                <label for="formula_roasting_<%= r %>">
                    <div class="img">
                        <%= image_tag "https://i.ytimg.com/vi/uZSrcVS6EuM/hqdefault.jpg" %>
                    </div>
                    <h6><%= r %></h6>
                </label>
            <% end %>
        </div>
    </div>
</div>
