<section>
    <div class="row">
        <div class="col-12">
            <h1>Ajouter une plage d'ouverture:</h1>

            <%= form_for :meal, :url => '/admin/meals' do |form| %>

                <p>
                    <%= form.label :start_time %><br>
                    <div class="select -multiple"><%= form.datetime_select :start_time, start_year: Time.current.year, default: 3.days.from_now %></div>
                </p>
                <p>
                    <%= form.label :end_time %><br>
                    <div class="select -multiple"><%= form.datetime_select :end_time, start_year: Time.current.year, default: 3.days.from_now %></div>
                </p>

                <p>
                    <%= form.button class: "btn" do %>
                        Créer <%= fa_icon "plus" %>
                    <% end %>
                </p>
            <% end %>
        </div>
    </div>
</section>
