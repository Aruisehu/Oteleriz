<section>
    <div class="row">

        <div class="col-12">
            <table>
                <tr>
                    <th>Début</th>
                    <th>Fin</th>
                    <th>Action</th>
                </tr>

                <% @meals.each do |meal| %>
                    <tr>
                        <td>
                            <%= meal.start_time.strftime("%A %d %B %H:%M") %>
                        </td>
                        <td>
                            <%= meal.end_time.strftime("%A %d %B %H:%M") %>
                        </td>
                        <td>

                            <a href="/admin/meals/<%= meal.id %>/edit">Changer les horaires</a>
                            <a href="/admin/meals/<%= meal.id %>" data-method="delete" rel="nofollow">delete</a>
                        </td>
                    </tr>
                    <% end %>
            </table>
        </div>
    </div>
</section>